$(document).ready(function(){

  // jQuery methods go here...

  // JQUERY METHODS TO TEST OUT:
  // 1) Adding elements when a button is pressed
  
  // 2) Run an animation when a button pressed (fading in/out, etc)
  // HINT: JQuery Effects

  // 3) When you hover over an element, change the elements styling
  // to appear different!

  // 4) When your mouse hovers over an image, hide the image and display
  // something new to the user. When the user leaves the hover, restore the 
  // image (use an animation)
  // --> use the on() event handler
  
  $("#hideElements").click(function(){
    $(".information").hide();
  })

  // Be willing to try things out!

});