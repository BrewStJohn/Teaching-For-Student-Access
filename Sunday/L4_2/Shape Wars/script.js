var c = document.getElementById("myCanvas");
var ctx = c.getContext("2d");

const player = {
    // PROPERTIES:
    // shape
    // x and y coordinates

    // METHODS:
    // draw 
    // changing its coordinates (update)
    // movement controls
}

// collision with squares