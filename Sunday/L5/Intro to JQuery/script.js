$(document).ready(function(){
    // 1) When you click on a paragraph element, hide it
    // 2) When you click a button, run an animation
    // on an element with a specific ID
    // 3) When you hover your mouse over an elememt, create/show
    // a new element
});